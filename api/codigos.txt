php artisan make:controller NomeDoController -> criar uma classe controller em laravel
php artisan make:model NomeDoModelo -> criar uma classe model em laravel


Controller:
O controlador é responsável por lidar com as requisições HTTP, executar a lógica de negócios e retornar as respostas apropriadas. Você pode criar um controlador da seguinte maneira:

Model:
O modelo é responsável por representar os dados e a lógica de negócios associada a uma entidade específica. Você pode criar um modelo da seguinte maneira:

View:
As views são responsáveis por exibir os dados e receber a interação do usuário. Crie as views correspondentes ao controlador da seguinte maneira:

php artisan serve